import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const StoryDetailScreen = () => {
	return (
		<View>
			<Text>StoryDetailScreen</Text>
		</View>
	);
};

export default StoryDetailScreen;

const styles = StyleSheet.create({});
